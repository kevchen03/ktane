# KTANE Solver - Working to Centurion
This app uses Vite with React.

## Usage
To start, run
```
npm install
```

You can run the following command to run locally: 
```
npm run dev
```

The server will start on [http://localhost:5173](http://localhost:5173).